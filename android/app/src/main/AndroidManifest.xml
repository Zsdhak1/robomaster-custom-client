<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Dart 与解码器播放器之间的本地 TCP 回环桥接需要该权限。 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 从 GitHub Release 下载 APK 更新需要该权限。 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <application
        android:label="WOD Client"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <!-- usesCleartextTraffic：MQTT (3333)、UDP 视频 (3334) 和本地 TCP 回环桥接
             都是 RoboMaster 裁判系统定义的明文 LAN 协议。Android 9+ 默认阻止明文流量，
             会破坏数据链路和设备端解码。 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- 指定 Android 进程启动后立即应用到该 Activity 的主题。
                 Flutter UI 初始化前用户会看到该主题；之后它继续决定 Flutter UI 背后的窗口背景。 -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- 不要删除下面的元数据。
             Flutter 工具会使用它生成 GeneratedPluginRegistrant.java。 -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- 需要查询可处理文本的 Activity，见：
         https://developer.android.com/training/package-visibility 和
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT。

         Flutter engine 的 io.flutter.plugin.text.ProcessTextPlugin 会使用该查询。 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
