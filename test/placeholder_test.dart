import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Phase 0 scaffolding', () {
    test('dependencies are resolvable', () {
      expect(true, isTrue);
    });

    test('protobuf messages are registered', () {
      // Verifies that generated protobuf classes compile correctly.
      expect(true, isTrue);
    });
  });
}
